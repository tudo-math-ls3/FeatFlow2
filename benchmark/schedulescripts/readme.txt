This folder allows to collect schedule scripts for tests to be run
in parallel on a cluster. Such scripts usually schedule a couple of jobs
with different maximum walltime which are then executed in parallel.
