#!/bin/sh
#
# This small shell script is used to create a default makefile
# for the application.
#

../../bin/configure \
	--appname=bio_app \
	--programfile=src/chemotaxis.f90 \
        --srclist_app="`ls src/*.f90`" \
	$@
